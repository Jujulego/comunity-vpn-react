import React, { FC } from 'react';

import { Typography } from '@material-ui/core';

// Component
const App: FC = () => {
  return (
    <div>
      <Typography>Hello world !</Typography>
    </div>
  );
};

export default App;
